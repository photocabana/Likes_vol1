var likeCount = 3;
var volume1 = document.querySelector("#count1");

function like() {
    likeCount++;
    volume1.innerText = likeCount + " like(s)";
    addLikes = document.querySelector(id)
}
